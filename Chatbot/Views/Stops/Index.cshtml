@model IEnumerable<Chatbot.Models.Stop>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table card">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Path.Start)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Path.End)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PathStop.Start)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PathStop.End)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Path.Start)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Path.End)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PathStop.Start)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PathStop.End)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
